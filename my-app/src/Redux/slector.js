// import { createSelector } from 'reselect';

// // Select the auth slice of the state
// const selectAuth = (state) => state.auth;

// // Define a selector to retrieve the clientInformation
// export const selectClientInformation = createSelector(
//   [selectAuth],
//   (auth) => auth.clientInformation
// );
